# From this directory: docker compose up -d --build
#
# The pma-fe image clones the front end from GitHub at build time (see Dockerfile.nginx).

services:
  mongo:
    image: mongo:7
    container_name: pma-mongo
    restart: unless-stopped
    volumes:
      # Host path: pma-be/production/data/mongo (relative to this compose file)
      - ../data/mongo:/data/db
    healthcheck:
      test: ['CMD', 'mongosh', '--quiet', '--eval', 'db.adminCommand({ ping: 1 })']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  pma-be:
    build:
      context: ../..
      dockerfile: production/docker/Dockerfile
    container_name: pma-be
    restart: unless-stopped
    volumes:
      # Host path: pma-be/production/data/files (relative to this compose file)
      - ../data/files:/app/files
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - '${PMA_BE_PORT:-5020}:5020'
    environment:
      SVC_ENV: prod
      NODE_ENV: production
      PORT: '5020'
      DBURL: mongodb://mongo:27017/pma-prod
      DISABLE_IPFS: 'true'
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '10'

  pma-fe:
    build:
      context: ../..
      dockerfile: production/docker/Dockerfile.nginx
      args:
        REACT_APP_PMA_SERVER: ${REACT_APP_PMA_SERVER:-http://localhost:5020}
        PMA_FE_REPO: ${PMA_FE_REPO:-https://github.com/christroutner/pma-fe.git}
        PMA_FE_GIT_REF: ${PMA_FE_GIT_REF:-main}
    container_name: pma-fe
    restart: unless-stopped
    # Default 8080 — port 80 is often taken by Apache/nginx on the host.
    ports:
      - '${PMA_FE_PORT:-8080}:80'
